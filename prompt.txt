我正在训练一个文本编辑模型，需要你帮我生成一些训练数据，训练数据包括:
1.一段原始内容
2.一段自然语言描述需要对文本做怎样的修改，此处的修改只涉及新增或者删除行、对于段文本的替换，描述需要非常准确没有异议，比如：删除"我是小草"这一行。
3.一段修改后的内容
4.从以下函数中挑选并处理上述问题并给出我对应的参数,
insert_behind_line(original_content,line_content,insert_content)
insert_before_line(original_content,line_content,insert_content)
replace_with_regex(original_content,regex_str,new_content)
replace_with_full_str(original_content,old_content,new_content)
delete_with_regex(original_content,regex_str)
delete_with_full_str(original_content,delete_content)
5.input的内容应该尽可能多样化，满足实际生产环境中的各种情况
6.input产生的内容一定可以被上述函数的功能满足，可以通过多次调用来达到最终结果，具体输出形式为[function_call_json1,function_call_json2...]

List of 8 tasks: